#ifndef OBJECT_H
#define OBJECT_H

/**
 * Returns an array containing the object public properties keys.
 */
void stdGetObjectKeys(struct result_list *args);

/**
 * Returns an array containing the object public properties values.
 */
void stdGetObjectValues(struct result_list *args);

#endif /* OBJECT_H */
